# Development only: Vite serves the UI on :5173 while the API runs on :8000, so the browser
# needs an absolute base. The production build ships with none and calls its own origin.
VITE_API_BASE=http://localhost:8000
